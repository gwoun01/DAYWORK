const path = require("path");
const webpack = require("webpack");
const dotenv = require("dotenv");
const fs = require("fs");

// ✅ 환경파일(.env) 로딩
const envPath = `.env.${process.env.NODE_ENV || "local"}`;
const fileEnv = fs.existsSync(envPath) ? dotenv.parse(fs.readFileSync(envPath)) : {};
const envKeys = Object.keys(fileEnv).reduce((acc, k) => {
  acc[`process.env.${k}`] = JSON.stringify(fileEnv[k]);
  return acc;
}, {});

// ✅ Webpack 설정
module.exports = {
  // ─────────────────────────────────────────────
  // ✅ 엔트리 포인트
  // ─────────────────────────────────────────────
  entry: {
    // 🖥️ PC 전용
    index: "./TypeScript/login/index.ts",              // 로그인 페이지 (index.html)
    workspace: "./TypeScript/workspace/00_workspace.ts",  // PC 워크스페이스 (workspace.html)
  },

  // ─────────────────────────────────────────────
  // ✅ 출력 설정
  // ─────────────────────────────────────────────
  output: {
    filename: "[name].bundle.js",                    // index.bundle.js, workspace.bundle.js, mobile-workspace.bundle.js
    path: path.resolve(__dirname, "docs/assets/js"), // 빌드 결과 저장 경로
    clean: false,                                    // 여러 번들 동시에 유지
  },

  // ─────────────────────────────────────────────
  // ✅ 모듈 해석 설정
  // ─────────────────────────────────────────────
  resolve: {
    extensions: [".ts", ".js"],                      // import 시 확장자 생략 허용
  },

  // ─────────────────────────────────────────────
  // ✅ TypeScript 로더
  // ─────────────────────────────────────────────
  module: {
    rules: [
      {
        test: /\.ts$/,
        use: {
          loader: "ts-loader",
          options: { configFile: "tsconfig.frontend.json" },
        },
        exclude: /node_modules/,
      },
    ],
  },

  // ─────────────────────────────────────────────
  // ✅ 최적화 (불필요한 청크 생성 방지)
  // ─────────────────────────────────────────────
  optimization: {
    splitChunks: false,
    runtimeChunk: false,
  },

  // ─────────────────────────────────────────────
  // ✅ 플러그인 (환경변수 주입)
  // ─────────────────────────────────────────────
  plugins: [new webpack.DefinePlugin(envKeys)],

  // ─────────────────────────────────────────────
  // ✅ 모드 및 소스맵
  // ─────────────────────────────────────────────
  mode: process.env.NODE_ENV === "production" ? "production" : "development",
  devtool: process.env.NODE_ENV === "production" ? false : "source-map",
};
